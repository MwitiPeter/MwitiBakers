{
  "name": "@mw/shared",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'Shared package linting is configured through parent packages.'"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "typescript": "^5.9.2"
  }
}
